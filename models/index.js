const categoriesBookModel = require('./categoriesBookModel');
const booksModel = require('./booksModel');
const usersModel = require('./usersModel');

module.exports = {
  categoriesBookModel,
  booksModel,
  usersModel,
};
